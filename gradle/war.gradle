apply plugin: "war"

bootWar {
    mainClassName = "kz.kaznu.nmm.aglomer.StorageApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
